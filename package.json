{
  "name": "bug-blaster-swarm-run",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check game.js && node --check api/scores.js && node scripts/rocket-balance-check.js && node scripts/core-visuals-check.js && node scripts/laser-hit-check.js && node scripts/player-health-check.js && node scripts/level-balance-check.js && node scripts/gasoline-ignition-check.js && node scripts/beetle-gold-ui-check.js"
  },
  "engines": {
    "node": "22.x"
  }
}
