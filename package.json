{
  "name": "bug-blaster-swarm-run",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check game.js && node --check api/scores.js"
  },
  "engines": {
    "node": "22.x"
  }
}
